// Domain tables land here as each owning slice ships (see docs/spec.md §6-§12).
// Empty for the walking skeleton: the migration pipeline is wired and proven
// against zero migrations, ready for the first real table.
export {};
